# Examples

`negentropy.perceives.examples` 按使用场景分层，作为包的子模块随 `pip install` 分发。

- `examples/configs/`：领域提取配置模板与辅助构建器
- `examples/mcp/`：MCP 工具与服务端调用示例
- `examples/sdk/`：Python SDK 集成示例

安装后可直接引用：

```python
from negentropy.perceives.examples.configs.extraction_configs import ECOMMERCE_CONFIG
```
